# admin.js バグ・問題分析レポート
**対象ファイル:** admin.js (4606行) / admin.html  
**分析日:** 2026-06-27

---

## 🔴 高（クラッシュ・データ破損リスク）

### BUG-01: `findMatch` / `findMatchByKey` が throw する設計なのに呼び出し元に try-catch なし
**場所:** L371-382、`finalizeMatchLocal`・`clearFinalLocal`・`selectMatch` 等多数  
**内容:** 無効な matchKey が渡された場合にJSエラーがスローされ、操作が完全に停止する。  
**修正:** `findMatchByKey` を `null` 返しに変更し、呼び出し元でnullチェックを追加する。

---

### BUG-02: 裏トーナメントのBYE確定が誤った配列を参照 ★最優先
**場所:** L2056  
```js
// 誤り
finalizeMatchLocal(currentEvent.matches, m, m.teamA ? "A" : "B", true);
// 正しい
finalizeMatchLocal(targetMatches, m, m.teamA ? "A" : "B", true);
```
**内容:** 裏トーナメントの組み合わせ編集中でも常に `currentEvent.matches`（メインブラケット）を参照。`currentEvent.loserMatches` を使うべき箇所で誤った配列が渡されデータが破損する。

---

### BUG-04: `clearOutcomeFrom` が循環参照で無限再帰になり得る
**場所:** L3921-3935  
**内容:** `nextMatchKey` を再帰的に追うが、壊れたデータがあるとスタックオーバーフロー。  
**修正:** 訪問済みキーの `Set` を保持して再訪を防ぐ。

---

### BUG-05: D&Dのグローバル変数 `window.__draggedTeam` が残り続ける
**場所:** L2953-3026付近（縦型）/ L3276-3290（横型）  
**内容:** ブラウザ外でマウスを放すと mouseup が届かず `__draggedTeam` がリセットされない。次の操作で意図しないスワップが発生する。  
**修正:** `document` に `mouseup` ハンドラを追加してリセット処理を仕掛ける。

---

### BUG-06: Undo スタックがイベント切り替え時にクリアされない ★最優先
**場所:** L271-302  
**内容:** イベントAの操作後にイベントBを開いてCtrl+Zを押すと、イベントAの状態に戻ってしまう。  
**修正:** `openEvent` 呼び出し時に `undoStack.length = 0; updateUndoUI();` を追加する。

---

### BUG-08: 複数の重要ボタンに null チェックなしで `.onclick` を直接設定
**場所:** L1111, L1113, L1291, L1300, L1310  
**対象:** `btnAddSport`, `btnSaveMeta`, `btnSaveSports`, `btnGenerateAll`, `btnReset`  
**内容:** IDが変わるとIIFE全体がクラッシュし、以降のバインドが全て失敗する。  
**修正:** 他のボタン同様 `if (el("btnXxx")) el("btnXxx").onclick = ...` のガードを付ける。

---

## 🟡 中（機能不全・誤動作）

### BUG-12: 再抽選（`btnReshuffle`）がブラケット枠数を誤計算する可能性
**場所:** L3885-3914  
**内容:** 手動でブラケット枠数を指定してから参加クラス数が変わると `r1count` と `bSize/2` が不一致になり、配列の上限外アクセスが発生する。  
**修正:** ループ上限を `r1count` ではなく `bSize / 2` に変更する。

---

### BUG-13: `clearFinalLocal` 後に `_origTeamA/B` が残り続ける
**場所:** L4052-4066  
**内容:** プレースホルダーフィールドが永続的にデータに残り、再びBYE判定される可能性がある。  
**修正:** 処理終了後に `delete lm._origTeamA; delete lm._origTeamB;` を追加する。

---

### BUG-17: `navSetup` の onclick が2箇所で設定され上書きが発生
**場所:** L951 / L4438  
**内容:** 元のハンドラを保存して呼び出す複雑な実装になっており、将来のバグの温床。  
**修正:** UI同期を `syncFormFromState` 内にまとめる。

---

### BUG-18: ウィザード系の要素がHTMLに存在しない（サイレント失敗）
**場所:** L949, L4270付近  
**対象:** `wNew`, `wGenerate`, `wClassList`, `wTName`, `wDate`, `wMatchMin`, `wIntervalMin`, `navWizard` 等  
**内容:** `wizardView` セクションがHTMLに存在しないため、ウィザード機能が全く動作しない（エラーも出ない）。

---

### BUG-20: `bracketInfoBanner` 要素がHTMLに存在しない
**場所:** L1412  
**内容:** ブラケット情報バナーが一切表示されない（`if (!banner) return` でサイレント失敗）。  
**修正:** admin.html に `<div id="bracketInfoBanner" class="hidden"></div>` を追加する。

---

### BUG-23: 裏トーナメントの `recalcLoserMatchTimes` がBYE試合をスキップしない
**場所:** L2346-2374  
**内容:** BYE試合にも `scheduledStart` が設定され、タイムテーブルに余分な行が表示される。  
**修正:** `if (m.isBye) { m.scheduledStart = null; continue; }` を追加する。

---

## 🔵 低（保守性・軽微な誤り）

| # | 内容 | 場所 |
|---|------|------|
| BUG-03 | エキシビション試合の slot 計算が全ラウンドを対象にしている | L2386 |
| BUG-10 | `shn()` の誤った呼び出しで TypeError の可能性 | L3357-3359 |
| BUG-11 | 横型ブラケットの UI パラメータ名が縦型と混在して直感的でない | L3153-3154 |
| BUG-14 | BYE分散配置の補完ループが先頭に偏る | L1454-1464 |
| BUG-21 | `idxClasses` という誤解を招く変数名（forEach の第1引数なのにインデックスのような命名） | L1276 |
| BUG-22 | `isMatchOngoing` / `isMatchOvertime` に重複ロジック（片方だけ修正すると不一致になる） | L719 / L3380 |
| BUG-24 | `setInterval` のコメント「30秒ごと」が実際は10秒と不一致 | L4417 |
| BUG-25 | `formatTeamName` の `replace("__", "")` が最初の一致のみ削除（replaceAll 推奨） | L145 |

---

## 優先修正まとめ

| 優先度 | バグ番号 | 内容 |
|--------|---------|------|
| 🔴 最優先 | BUG-02 | 裏トーナメントBYEがメインブラケット配列を誤参照 |
| 🔴 最優先 | BUG-06 | Undoスタックがイベント間で混入 |
| 🔴 高 | BUG-05 | D&Dグローバル変数がリセットされない |
| 🔴 高 | BUG-04 | clearOutcomeFromが無限再帰になり得る |
| 🟡 中 | BUG-18 | ウィザード機能がHTML不在で全滅 |
| 🟡 中 | BUG-20 | ブラケット情報バナーがHTML不在で非表示 |
| 🟡 中 | BUG-23 | 裏トーナメントにBYEスキップ処理がない |
| 🟡 中 | BUG-13 | _origTeamA/B が永続的に残る |
